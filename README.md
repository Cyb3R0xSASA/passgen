# passgen
CLI program to generate random password.

## install as linux tool
```bash
chmod +x app.js
npm link
```

> `npm link` add it as user binary app.

## Using from anywhere with your user
```bash
passgen <options>
```